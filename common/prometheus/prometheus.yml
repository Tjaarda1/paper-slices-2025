global:
  scrape_interval: 15s     # default
  evaluation_interval: 15s # rules/alerts, if you add them

scrape_configs:
  - job_name: node
    static_configs:
      - targets:
          - localhost:9100   # Node Exporter on the same machine
